import styled from 'styled-components';
import Grid from '@material-ui/core/Grid';

const LoadingScreenWrapper = styled(Grid)`
  margin-top: 2em;
  max-width: 600px;
`;


export {
  LoadingScreenWrapper
}
